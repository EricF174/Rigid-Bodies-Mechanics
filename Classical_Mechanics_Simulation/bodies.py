import numpy as np


class particle:
    def __int__(self):
        self.position = np.array([])
        self.velocity = np.array([])
        self.acceleration = np.array([])
        self.mass = np.array([])
